# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1
set $gapsize 16

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Roboto, FontAwesome 10.4

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Workspace layout
workspace_layout default

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec "termite"

# start standard applications
bindsym $mod+Control+1 exec firefox
bindsym $mod+Control+2 exec "termite"
bindsym $mod+Control+3 exec gedit
bindsym $mod+Control+4 exec "termite -e weechat --role='weechatterminal' --title='weechat'"
bindsym $mod+Control+5 exec "rambox"
bindsym $mod+Control+6 exec "termite -e mutt --role='muttterminal' --title='mutt'"
bindsym $mod+Control+7 exec anki
bindsym $mod+Control+8 exec jabref
bindsym $mod+Control+9 exec filezilla
bindsym $mod+Control+0 exec nautilus

# focus standard applications on key
bindsym control+1 [class="Firefox"] focus
bindsym control+2 [title="termite"] focus
bindsym control+3 [title="gedit"] focus
bindsym control+4 [title="weechat"] focus
bindsym control+5 [title="Rambox"] focus
bindsym control+6 [title="mutt"] focus
bindsym control+7 [class="Anki"] focus
bindsym control+8 [title="Jabref"] focus
bindsym control+9 [title="Filezilla"] focus
bindsym control+0 [class="Nautilus"] focus

# kill focused window
bindsym $mod+Shift+q kill

# start rofi as a dmenu replacement
bindsym $mod+d exec rofi -show run -modi run

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+odiaeresis focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+odiaeresis move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+Shift+a focus parent

# focus the child container
#bindsym $mod+d focus child

# assign workspace variables
set $WS1 "1:<span font='10.4'> >_ </span>"
set $WS2 "2:<span font='10.4'> 🌏 </span>"
set $WS3 "3:<span font='10.4'> 🌓 </span>"
set $WS4 "4:<span font='10.4'> 🗨  </span>"
set $WS5 "5:<span font='10.4'> 🖿  </span>"
set $WS6 "6:<span font='10.4'> 🗫 </span>"
set $WS7 "7:<span font='10.4'> 🔁 </span>"
set $WS8 "8:<span font='10.4'> 🕮 </span>"
set $WS9 "9:<span font='10.4'> 9 </span>"
set $WS10 "10:<span font='10.4'> 10 </span>"

# switch to workspace
bindsym $mod+1 workspace $WS1
bindsym $mod+2 workspace $WS2
bindsym $mod+3 workspace $WS3
bindsym $mod+4 workspace $WS4
bindsym $mod+5 workspace $WS5
bindsym $mod+6 workspace $WS6
bindsym $mod+7 workspace $WS7
bindsym $mod+8 workspace $WS8
bindsym $mod+9 workspace $WS9
bindsym $mod+0 workspace $WS10

# assign workplaces to screens
# workspace $WS1 output HDMI-1
# workspace $WS2 output HDMI-1
# workspace $WS3 output HDMI-1
# workspace $WS4 output HDMI-1
# workspace $WS5 output DVI-I-1
# workspace $WS6 output DVI-I-1
# workspace $WS7 output DVI-I-1
# workspace $WS8 output DVI-I-1

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $WS1
bindsym $mod+Shift+2 move container to workspace $WS2
bindsym $mod+Shift+3 move container to workspace $WS3
bindsym $mod+Shift+4 move container to workspace $WS4
bindsym $mod+Shift+5 move container to workspace $WS5
bindsym $mod+Shift+6 move container to workspace $WS6
bindsym $mod+Shift+7 move container to workspace $WS7
bindsym $mod+Shift+8 move container to workspace $WS8
bindsym $mod+Shift+9 move container to workspace $WS9
bindsym $mod+Shift+0 move container to workspace $WS10

# Start standard applications on specified workspaces
# Assign FF terminals to workspace 2
assign [class="Firefox"] $WS2
assign [title="gedit"] $WS1
assign [class="Filezilla"] $WS1
assign [class="Rambox"] $WS4
assign [title="weechat"] $WS4
assign [class="Nautilus"] $WS5
assign [class="Icedove"] $WS6
assign [class="JabRef"] $WS8

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+u+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# mpd keybindings
bindsym XF86AudioPlay exec mpc -p 6601 toggle
bindsym $mod+XF86AudioPlay exec termite -e "ncmpcpp -p 6601"
bindsym XF86AudioStop exec mpc -p 6601 stop
bindsym XF86AudioNext exec mpc -p 6601 next
bindsym XF86AudioPrev exec mpc -p 6601 prev

bindsym Shift+XF86AudioPlay exec mpc -h 192.168.2.102 -p 6601 toggle
bindsym $mod+Shift+XF86AudioPlay exec termite -e "ncmpcpp -h 192.168.2.102 -p 6601"
bindsym Shift+XF86AudioStop exec mpc -h 192.168.2.102 -p 6601 stop
bindsym Shift+XF86AudioNext exec mpc -h 192.168.2.102 -p 6601 next
bindsym Shift+XF86AudioPrev exec mpc -h 192.168.2.102 -p 6601 prev

bindsym XF86AudioLowerVolume exec --no-startup-id amixer set Master 3%- 
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer set Master 3%+
bindsym XF86AudioMute exec amixer -q -D pulse set Master toggle

bindsym $mod+control+e exec "~/Sync/Programming/Python/checkmaildir/sendnotif.sh checkmaildirs.py"
bindsym $mod+Shift+u exec "~/Sync/Programming/Python/checkmaildir/sendnotif.sh checkmail_files.py"
bindsym $mod+Shift+o exec termite -e "w3m wttr.in"

# Assistive mode
set $mode_assistant Read time (q), date (Shift+q), next task (w), mail (e), weather (t)
Mode "$mode_assistant" {
    bindsym q exec ~/Sync/Programming/Python/texttospeech/texttospeech.py date hour, mode "default"
    bindsym Shift+q exec ~/Sync/Programming/Python/texttospeech/texttospeech.py date, mode "default"
    bindsym control+q exec ~/Sync/Programming/Python/texttospeech/texttospeech.py date day, mode "default"
    bindsym w exec ~/Sync/Programming/Python/texttospeech/texttospeech.py task, mode "default"
    bindsym e exec ~/Sync/Programming/Python/texttospeech/texttospeech.py mail subjects, mode "default"
    bindsym t exec ~/Sync/Programming/Python/texttospeech/texttospeech.py weather, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+e mode "$mode_assistant"

# Gaps modes
set $mode_gaps Gaps: (O) Outer, (I) Inner
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"
mode "$mode_gaps" {
        bindsym Escape mode "default"
        bindsym Return mode "default"
        bindsym o      mode "$mode_gaps_outer"
        bindsym i      mode "$mode_gaps_inner"
        bindsym Shift+o      mode "$mode_gaps_outer"
        bindsym Shift+I      mode "$mode_gaps_inner"
}

mode "$mode_gaps_inner" {
        bindsym plus  gaps inner current plus 5
        bindsym minus gaps inner current minus 5
        bindsym 0     gaps inner current set 0

        bindsym Shift+plus  gaps inner all plus 5
        bindsym Shift+minus gaps inner all minus 5
        bindsym Shift+0     gaps inner all set 0

        bindsym Return mode "default"
        bindsym Escape mode "default"
}

mode "$mode_gaps_outer" {
    bindsym plus gaps outer current plus 5
    bindsym minus gaps outer current minus 5
    bindsym 0     gaps outer current set 0

    bindsym Shift+plus  gaps outer all plus 5
    bindsym Shift+minus gaps outer all minus 5
    bindsym Shift+0     gaps outer all set 0

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

set $Locker i3lock -i /home/jrenslin/Sync/Images/Wallpapers/Lockscreen/Singapore_street_III[KaiLehmann][CC-BY]_1920_1080.png -t && sleep 1
# set $Locker i3lock -i /home/jrenslin/Sync/Images/Wallpapers/Wallpaper_Colors.png -t && sleep 1
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" { 

    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff -i, mode "default"  

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+control+p mode "$mode_system"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym odiaeresis resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {

    i3bar_command i3bar

    #status_command i3status 
	# --config ~/.config/i3/i3status.conf

    status_command i3blocks -c ~/.config/i3blocks/config_workstation
    #status_command py3status -c ~/.config/i3/i3status.conf
    # status_command python /usr/lib/python3.4/site-packages/powerline/bindings/i3/powerline-i3.py

    id	bar-1
    font pango:FontAwesome, Roboto	9.8
    workspace_buttons	yes
    strip_workspace_numbers	yes    
    tray_output	primary
    tray_padding	7
    position	bottom
    height 32

    bindsym button2 exec "termite -e newsbeuter"
    bindsym button3 exec termite -e "ncmpcpp -h 192.168.2.102 -p 6601"

    separator_symbol  " "

    colors {

        background #eeeeee
        statusline #888888
        separator  #0087ff

        focused_workspace  #0087ff #0087ff #ffffff
        active_workspace   #dddddd #dddddd #dddddd
        inactive_workspace #eeeeee #eeeeee #888888
        urgent_workspace   #FF0054 #FF0054 #ffffff
        binding_mode       #0087ff #0087ff #ffffff

    }
}

# Gaps

gaps inner $gapsize
gaps outer 1
# smart_gaps on
# smart_borders on

# Color scheme
# class                 border  backgr. text    indicator child_border
client.focused          #0087ff #0087ff #ffffff #292d2e   #ffffff
client.focused_inactive #0087ff #0087ff #888888 #333333   #ffffff
client.unfocused        #eeeeee #f0f7fe #888888 #333333   #ffffff
client.urgent           #900000 #900000 #ffffff #900000   #ffffff
client.placeholder      #eeeeee #0c0c0c #ffffff #000000   #ffffff
client.background       #ffffff

# Set window properties
new_window pixel 1
for_window [class="Rambox"] resize shrink 800px

# Startup
exec_always "xscreensaver -c"
exec "xrandr --output HDMI-1 --primary --left-of DVI-I-1"
exec "mpd"
exec --no-startup-id "xcompmgr &"
exec --no-startup-id "sleep 1; emacs --daemon"
exec "termite"

exec --no-startup-id "sleep 1; conky -c ~/.config/conky/load-rarely/conky-clock.conf"
exec --no-startup-id "sleep 1; conky -c ~/.config/conky/load-rarely/conky-mpd.conf"
exec --no-startup-id "sleep 1; conky -c ~/.config/conky/tiles/conky-files-sync.conf"
exec --no-startup-id "sleep 1; conky -c ~/.config/conky/tiles/conky-files-notes.conf"
exec --no-startup-id "sleep 1; conky -c ~/.config/conky/tiles/conky-files-pictures.conf"
exec --no-startup-id "sleep 1; conky -c ~/.config/conky/tiles/conky-files-cache.conf"

exec "syncthing"
exec --no-startup-id "feh --bg-scale ~/Sync/Images/Wallpapers/desktop_light/Bicycle\[Wenchieh_Yang\]\[PD\].jpg &"
exec "rambox"
exec "termite -e weechat --role='weechatterminal' --title='weechat'"
exec "offlineimap &"

exec xautolock -time 10 -locker "$Locker -t" -detectsleep 

